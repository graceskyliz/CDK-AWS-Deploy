{
  "name": "ecs-fargate-cdk",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "cdk": "cdk",
    "deploy": "npm run build && cdk deploy --require-approval never",
    "destroy": "cdk destroy --force"
  },
  "dependencies": {
    "aws-cdk-lib": "2.153.0",
    "constructs": "10.3.0",
    "dotenv": "16.4.5"
  },
  "devDependencies": {
    "ts-node": "10.9.2",
    "typescript": "5.4.5"
  }
}
